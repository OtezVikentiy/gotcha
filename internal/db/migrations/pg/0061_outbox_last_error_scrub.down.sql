-- Откатывать нечего: заменённый текст ошибки не восстановим (да и не должен
-- быть — в нём были секреты). Пустой down нужен, чтобы миграция была
-- обратимой для тестов up/down/up (см. 0025_outbox_drop_secret.down.sql —
-- тот же приём для того же необратимого по смыслу UPDATE).
SELECT 1;
