-- retries — сколько раз повторить одну проверку монитора при неуспехе,
-- прежде чем записать её как сбой (0 = без повторов). Гасит транзиентные
-- блипы (например периодический TLS-тарпит фронта, проходящий на повторе).
ALTER TABLE monitors ADD COLUMN retries integer NOT NULL DEFAULT 0;
