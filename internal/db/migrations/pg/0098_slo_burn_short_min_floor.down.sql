-- Откатывать нечего: какое значение было ниже 5 до up, не сохранено, а
-- поднятое значение и не должно откатываться — оно стало верным, не неверным.
-- Пустой down тем же приёмом, что 0061_outbox_last_error_scrub.down.sql.
SELECT 1;
