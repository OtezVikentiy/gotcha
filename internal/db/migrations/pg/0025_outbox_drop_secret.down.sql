-- Откатывать нечего: удалённые секреты не восстановимы (да и не должны быть).
-- Задачи, оставшиеся в очереди без 'secret', доставит и старый, и новый код —
-- новый резолвит секрет по channel_id, старый на пустом секрете провалит
-- отправку и уйдёт в ретрай. Пустой down нужен, чтобы миграция была обратимой
-- для тестов up/down/up.
SELECT 1;
