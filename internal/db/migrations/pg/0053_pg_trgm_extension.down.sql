-- Расширение снимается только после того, как оба зависящих от него индекса
-- (0054/0055) уже сняты своими собственными down-миграциями — иначе DROP
-- EXTENSION упал бы на зависимости (индексы gin_trgm_ops ссылаются на
-- операторный класс расширения).
DROP EXTENSION IF EXISTS pg_trgm;
